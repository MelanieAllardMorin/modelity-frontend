import L from "leaflet";

export const departureMarker = L.icon({
  iconUrl: "../assets/departure.svg",
  iconSize: [30, 30]
});

export const destinationMarker = L.icon({
  iconUrl: "../assets/destination.svg",
  iconSize: [30, 30]
});